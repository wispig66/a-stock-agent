# Example configuration for pull-based remote Codex deployment.
# Copy to deploy.remote.env and adjust values for your private host.

REMOTE_HOST=deploy@example.com
REMOTE_ROOT=/opt/stock
REMOTE_REPO_URL=https://github.com/example/stock.git
REMOTE_BRANCH=main
REMOTE_RUN_TESTS=1
